<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application
    android:allowBackup="false"
    android:usesCleartextTraffic="false">
    <!-- Woken by Play Services when a DataItem lands under /workout, even if
         the app isn't running — matches the offline write-ahead-log flush.
         Also woken by the watch's /request-sync Message (on-demand sync). -->
    <service
      android:name="expo.modules.wearsync.WearSyncListenerService"
      android:exported="true">
      <intent-filter>
        <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
        <data
          android:scheme="wear"
          android:host="*"
          android:pathPrefix="/workout" />
      </intent-filter>
      <intent-filter>
        <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
        <data
          android:scheme="wear"
          android:host="*"
          android:pathPrefix="/request-sync" />
      </intent-filter>
    </service>
  </application>
</manifest>
